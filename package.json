{
  "name": "qa-healthtech-pipeline",
  "version": "1.0.0",
  "description": "Pipeline de testes automatizados E2E e API para aplicação Healthtech",
  "main": "index.js",
  "scripts": {
    "test:api": "cd bruno && npx @usebruno/cli run --env Production",
    "test:e2e": "cypress run",
    "test:all": "npm run test:api && npm run test:e2e"
  },
  "devDependencies": {
    "@usebruno/cli": "^1.26.0",
    "cypress": "^13.0.0"
  }
}
